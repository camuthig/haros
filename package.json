{
  "name": "haros",
  "version": "0.1.0",
  "description": "A package for managing an API gateway server.",
  "main": "./dist/gateway.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/babel --presets es2015 lib -d dist",
    "build:watch": "watch 'npm run build' lib",
    "prepublish": "npm run build"
  },
  "keywords": [
    "api",
    "gateway"
  ],
  "author": "Chris Muthig",
  "license": "MIT",
  "dependencies": {
    "enumify": "^1.0.4",
    "express": "^4.13.4",
    "jsonwebtoken": "^7.0.0",
    "lodash": "^4.13.1",
    "mongoose": "^4.4.20",
    "rocky": "^0.4.12",
    "validate.js": "^0.10.0"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "watch": "^0.18.0"
  }
}
